// Numeric if-then-else
(If 0 t f) = f
(If n t f) = t

// Applies a function N times to an argument
(Apply 0 f x) = x
(Apply n f x) = (Apply (/ n 2) λk(f (f k)) ((If (% n 2) f (λx x)) x))

// Applies `id` 1k times to an argument
(Kid x) = (Apply 1000 λx(x) x)

// Applies `Kid` 10m times to 42
(Main) = (Apply 10000000 λx(Kid x) 42)
