[package]
name = "hvm"
version = "2.0.6"
edition = "2021"
build = "build.rs"
description = "A parallel virtual machine."
license = "MIT"

[dependencies]
TSPL = "0.0.12"
clap = "4.5.2"
num_cpus = "1.0"

[build-dependencies]
cc = "1.0"
num_cpus = "1.0"
