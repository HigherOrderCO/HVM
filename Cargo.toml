[package]
name = "hvm"
version = "2.0.8"
edition = "2021"
build = "build.rs"
description = "A massively parallel, optimal functional runtime in Rust."
license = "Apache-2.0"

[dependencies]
TSPL = "0.0.12"
clap = "4.5.2"
num_cpus = "1.0"

[build-dependencies]
cc = "1.0"
num_cpus = "1.0"
